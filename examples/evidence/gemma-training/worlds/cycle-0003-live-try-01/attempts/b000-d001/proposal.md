This solution uses a min-heap to efficiently merge the intervals. The heap is initialized with the start times of the intervals.  Each interval is popped from the heap, and if it overlaps with the last merged interval (or touches), the end time of the last merged interval is updated. If it doesn't overlap, the merged interval is added to the result. This approach has a time complexity of O(n log n) due to the heap operations.
